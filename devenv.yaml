inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixpkgs-unstable
  nixpkgs-python:
    url: github:cachix/nixpkgs-python

# Allow unfree packages if needed (some Python packages might require this)
allowUnfree: true
